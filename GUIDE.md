# 📖 راهنمای جامع راه‌اندازی و استفاده از Antigravity Mobile Remote

این راهنما مراحل گام‌به‌گام نصب، راه‌اندازی، تنظیم فایروال، اتصال گوشی موبایل و رفع مشکلات احتمالی را برای دو سیستم‌عامل **ویندوز (Windows)** و **لینوکس (Linux)** شرح می‌دهد.

---

## 📑 فهرست مطالب
1. [پیش‌نیازها](#-پیش‌نیازها)
2. [راه‌اندازی سریع با منوی نصب هوشمند](#-راه‌اندازی-سریع-با-منوی-نصب-هوشمند)
3. [نصب و راه‌اندازی در لینوکس](#-نصب-و-راه‌اندازی-در-لینوکس)
4. [نصب و راه‌اندازی در ویندوز](#-نصب-و-راه‌اندازی-در-ویندوز)
5. [کلیدهای میانبر و دستورات ترمینال](#-کلیدهای-میانبر-و-دستورات-ترمینال)
6. [نحوه اتصال گوشی موبایل و نصب PWA](#-نحوه-اتصال-گوشی-موبایل-و-نصب-pwa)
7. [تنظیمات فایروال و شبکه](#-تنظیمات-فایروال-و-شبکه)
8. [عیب‌یابی و پرسش‌های متداول](#-عیب‌یابی-و-پرسش‌های-متداول)

---

## 📋 پیش‌نیازها

پیش از شروع، مطمئن شوید موارد زیر روی سیستم شما فراهم است:

1. **Node.js (نسخه ۱۸ یا بالاتر):**
   - دانلود از سایت رسمی [nodejs.org](https://nodejs.org) یا نصب از طریق پکیج منیجر توزیع لینوکس شما.
2. **مدیر بسته npm:** همراه با نصب Node.js به صورت پیش‌فرض نصب می‌شود.
3. **ابزار خط فرمان Antigravity (`agy` CLI):**
   - اطمینان حاصل کنید که `agy` در مسیر سیستم (`PATH`) شما یا در شیم‌های `mise` قرار دارد.
4. **اتصال Wi-Fi مشترک:** کامپیوتر و گوشی موبایل باید به یک مودم یا شبکه Wi-Fi یکسان متصل باشند.

---

## ⚡ راه‌اندازی سریع با منوی نصب هوشمند

پروژه دارای یک نصاب خودکار و تعاملی است که کلیه پیش‌نیازها (نسخه Node، دسترسی npm، شناسایی موتور `agy`، آزاد بودن پورت 7788 و آی‌پی محلی) را بررسی کرده و با تایید شما اقدامات لازم را انجام می‌دهد:

### در لینوکس یا مک:
```bash
./install.sh
```

### در ویندوز:
روی فایل `install.bat` دابل‌کلیک کنید، یا در PowerShell دستور زیر را بزنید:
```powershell
.\install.ps1
```

### یا مستقیماً از طریق Node.js:
```bash
node scripts/installer.js
```

منوی نصب مراحل زیر را با شما بررسی می‌کند:
- بررسی پیش‌نیازهای ۵ گانه سیستم
- درخواست تایید برای نصب وابستگی‌ها (`npm install`)
- درخواست تایید برای بیلد برنامه با esbuild (`npm run build`)
- درخواست تایید برای ثبت دستور سراسری (`npm link`) جهت امکان اجرای `antigravity-remote` در هر دایرکتوری
- پیکربندی سرویس پس‌زمینه (systemd در لینوکس / ایجاد فایل راه‌انداز در ویندوز)
- اجرای آنی سرور در صورت تمایل شما

---

## 🐧 نصب و راه‌اندازی در لینوکس

### ۱. نصب دستی و بیلد
در صورتی که نمی‌خواهید از اسکریپت خودکار استفاده کنید:
```bash
# رفتن به پوشه پروژه
cd /path/to/antigravity-mobile-remote

# نصب وابستگی‌ها
npm install

# بیلد پروژه
npm run build

# اجرای برنامه
npm start
```

### ۲. ثبت دستور سراسری
برای اینکه بتوانید در هر پوشه‌ای با زدن دستور `antigravity-remote` برنامه را اجرا کنید:
```bash
npm link
```
سپس در هر پروژه‌ای کافیست دستور زیر را بزنید:
```bash
antigravity-remote
```

### ۳. اجرای خودکار به عنوان سرویس پس‌زمینه (Systemd User Service)
فایل سرویس آماده در ریشه مخزن با نام [antigravity-mobile-remote.service](./antigravity-mobile-remote.service) قرار دارد. برای فعال‌سازی در کاربر جاری:
```bash
mkdir -p ~/.config/systemd/user
cp antigravity-mobile-remote.service ~/.config/systemd/user/
systemctl --user daemon-reload
systemctl --user enable --now antigravity-mobile-remote.service
```

مشاهده وضعیت و لاگ‌ها:
```bash
systemctl --user status antigravity-mobile-remote
journalctl --user -u antigravity-mobile-remote -f
```

---

## 🪟 نصب و راه‌اندازی در ویندوز

### ۱. نصب و بیلد
در محیط Command Prompt یا PowerShell:
```cmd
cd path\to\antigravity-mobile-remote
npm install
npm run build
npm start
```

### ۲. ساخت فایل اجرایی تک‌کلیک
پس از بیلد، می‌توانید یک فایل `run.bat` با محتوای زیر بسازید و آن را روی دسکتاپ قرار دهید:
```cmd
@echo off
title Antigravity Mobile Remote
cd /d "C:\path\to\antigravity-mobile-remote"
node dist\cli.js
pause
```

---

## ⌨️ کلیدهای میانبر و دستورات ترمینال

هنگامی که سرور در حال اجرا است، بدون نیاز به ری‌استارت می‌توانید مستقیماً در ترمینال با کلیدهای زیر تعامل کنید:

| کلید | عملکرد |
| :---: | :--- |
| **`r`** | چاپ مجدد کد QR و آدرس‌های اتصال جهت اسکن مجدد |
| **`o`** | باز کردن خودکار آدرس سرور محلی در مرورگر دسکتاپ سیستم |
| **`c`** | پاک کردن تاریخچه سشن گفتگوها (Clear History) |
| **`s`** | نمایش خلاصه وضعیت، تعداد گوشی‌های متصل و رم مصرفی |
| **`w`** | نمایش مسیر دایرکتوری کاری فعال (Workspace) |
| **`h`** | نمایش راهنمای کلیدها |
| **`q`** یا **`Ctrl+C`** | توقف کامل، آزادسازی پورت و خروج ایمن از برنامه |

### سوییچ‌های خط فرمان (CLI Flags):
```bash
antigravity-remote --port 8080 --effort high --workspace /home/user/my-project
```
- `-p, --port <number>`: تغییر پورت پیش‌فرض (پیش‌فرض: 7788)
- `-w, --workspace <path>`: پوشه مقصد برای پروژه‌ای که ایجنت در آن کار می‌کند
- `-e, --effort <low|medium|high>`: تغییر پیش‌فرض درجه تفکر ایجنت (پیش‌فرض: `low`)
- `-o, --open`: باز کردن خودکار مرورگر به محض اجرا
- `--no-qr`: جلوگیری از چاپ کد QR (برای محیط‌هایی که از کاراکترهای یونیکد پشتیبانی نمی‌کنند)

---

## 📱 نحوه اتصال گوشی موبایل و نصب PWA

1. دوربین گوشی (مثلاً Samsung Galaxy S24) را به سمت کد QR نمایش‌داده‌شده در ترمینال بگیرید.
2. روی لینکی که ظاهر می‌شود ضربه بزنید (به شکل `http://192.168.x.x:7788`).
3. صفحه کنترل وب باز می‌شود و به صورت خودکار از طریق WebSocket متصل می‌شود.
4. **نصب به عنوان اپلیکیشن بدون مرز (PWA):**
   - در مرورگر Chrome یا Samsung Internet، روی منوی سه‌نقطه بزنید.
   - گزینه **«Add to Home screen»** یا **«Install app»** را انتخاب کنید.
   - اکنون آیکون Antigravity Remote مانند یک نرم‌افزار نیتیو روی صفحه اصلی گوشی شما قرار می‌گیرد و با تمام‌صفحه باز می‌شود.

---

## 🛡️ تنظیمات فایروال و شبکه

برای اینکه گوشی بتواند به پورت 7788 کامپیوتر متصل شود، ممکن است لازم باشد پورت در فایروال مجاز شود:

### در لینوکس (UFW):
```bash
sudo ufw allow 7788/tcp
sudo ufw reload
```

### در ویندوز (Windows Defender Firewall):
دستور زیر را در PowerShell در حالت **Run as Administrator** اجرا کنید:
```powershell
New-NetFirewallRule -DisplayName "Antigravity Mobile Remote" -Direction Inbound -LocalPort 7788 -Protocol TCP -Action Allow
```

---

## ❓ عیب‌یابی و پرسش‌های متداول

### ۱. گوشی صفحه را باز نمی‌کند یا خطای Connection Timed Out می‌دهد:
- مطمئن شوید هر دو دستگاه به یک شبکه Wi-Fi متصل هستند.
- قابلیت «AP Isolation» یا «Client Isolation» را در تنظیمات مودم بررسی کنید (باید غیرفعال باشد تا دستگاه‌های داخل شبکه بتوانند یکدیگر را ببینند).
- فایروال سیستم میزبان را طبق بخش بالا تنظیم کنید.

### ۲. خطای `agy not found in PATH`:
- بررسی کنید آیا Antigravity را از طریق mise یا npm یا مسیر سفارشی نصب کرده‌اید.
- می‌توانید با اضافه کردن مسیر پوشه باینری به متغیر محیطی PATH این مشکل را برطرف کنید.

### ۳. پورت 7788 درگیر است:
- می‌توانید با سوییچ `-p` پورت دیگری را مشخص کنید:
  ```bash
  antigravity-remote -p 8899
  ```
